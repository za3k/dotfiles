# set vim to be the default editor
#TODO: Make this work
export WHICHVIM=vim # preferred vim (vim or gvim)
export EDITOR=$WHICHVIM
if [ -n "$DISPLAY" ] && which chromium 2>/dev/null >/dev/null; then
  export BROWSER=chromium
else
  export BROWSER=elinks
fi
